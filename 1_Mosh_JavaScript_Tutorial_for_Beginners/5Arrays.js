//arrays (is a data Structure Represent a list of item)
let selectedColour = ['red', 'blue'];
console.log(selectedColour);

//Objects and size in arrays are dynamic
selectedColour[2] = 1;
console.log(selectedColour);

//types of array is object
//so we can use Dot Notation to perform Diff properties
//example
console.log(selectedColour.length);     //3
